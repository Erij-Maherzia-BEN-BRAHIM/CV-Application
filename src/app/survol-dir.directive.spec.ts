import { SurvolDirDirective } from './survol-dir.directive';

describe('SurvolDirDirective', () => {
  it('should create an instance', () => {
    const directive = new SurvolDirDirective();
    expect(directive).toBeTruthy();
  });
});
